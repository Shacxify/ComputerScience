import java.util.*;
/**
 * Write a description of class SumOfBlocks here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class SumOfBlocks
{
    public static void main(String[] args){
        Scanner input = new Scanner(System.in);
        System.out.println("Enter a number of floors: ");
        int limit = input.nextInt();
        int rooms = 0;
        int floor = 0;
        System.out.println("The number of rooms is " +getRooms(limit, floor, rooms));

    }

    public static int getRooms(int limit, int floor, int rooms){
        if (limit == 0){
            return 0;
        }
        floor += 1;
        rooms += floor;
        if(floor >= limit ){
            return rooms;
        }

        else{
            return getRooms(limit, floor, rooms);
        }
    }
}

